# task-{name}-{YYYYMMDD}.md

> `/design` 산출물. 자연어 설계만 담는다. 타입·Endpoint·에러코드는 계약 파일이 정본이므로 여기 중복 작성 금지.
> **제시 순서 = 수정 가능성 순**: 판단이 갈리는 것(사각지대·결정)을 앞에, 기계적 작업(File Map 배선)을 뒤에 둔다. 실행 순서가 아니라 "주의를 기울일 것 먼저".

## 1. Requirements

- **Scenario**: {{어떤 상황에서 누가 무엇을 하는가}}
- **Objective**: {{이 작업이 달성하려는 것}}
- **Acceptance Criteria** (측정 가능, 3개 이상):
  - [ ] AC1: {{예: 목록 API가 200ms 이내 응답}}
  - [ ] AC2: {{예: 잘못된 파라미터 시 EX002 반환}}
  - [ ] AC3: {{...}}

## 2. 사각지대 & 핵심 결정 (수정 가능성 순 — 새 기능·불확실성 있을 때)

> `/design`의 사각지대 패스에서 나온 것. 자명한 작업이면 생략 가능. 여기가 "가장 주의를 기울일 것".

- **사각지대(부딪히면 배울 함정)**: {{예: 세션은 SessionBridge로 이중 기록됨 — RedisStore에 직접 쓰면 dev만 통과. revert된 PR #2841 참고}}
- **핵심 결정** (택한 안 + 기각한 대안):
  - **결정 1 — {{예: 스냅샷 vs 실시간 조인}}**: 택함 = {{비정규화 스냅샷}} / 기각 = {{실시간 JOIN — 과거가 다시 쓰임}} / 사유 = {{내보내기는 그 시점 기록이어야}}
  - **결정 2 — {{...}}**: 택함 / 기각 / 사유

## 3. UI/UX (해당 시)

{{레이아웃·인터랙션·상태 흐름}}

## 4. Logic

{{핵심 알고리즘·계산식·쿼리 개요. 자연어 + 필요 시 의사코드}}

## 5. Implementation Split (해당 시)

- **BE**: {{책임}}
- **FE**: {{책임}}

## 6. File Map (기계적 — 판단 필요 없음)

- `[New] {{경로}}` — {{역할}}
- `[Mod] {{경로}}` — {{변경 내용}}

## 7. Verification

- 명령: {{예: `npm test -- example`}}
- 통과 조건: Exit 0, {{AC 매핑}}

## 8. History

`/builder`·`/sync`가 갱신. 구현 중 **계획에서 벗어난 지점은 이탈(Deviation)로 기록** — 계획이 말한 것 / 코드가 드러낸 것 / 택한 선택 / 사유.

| 일시 | 단계 | 내용 |
|:---|:---|:---|
| {{YYYYMMDD HH:MM}} | /design | 최초 설계 |
| {{YYYYMMDD HH:MM}} | /builder · Deviation | {{예: 레거시 주석에 frame_ts 없음 → 계획은 '항상 있음' 가정. 있는 것만 정렬하고 없으면 0 처리(보수적). 사유: 마이그레이션 전 데이터 혼재}} |
